name: programming_news_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  chaleno: ^0.0.6
  mobx: ^2.2.3+1
  flutter_mobx: ^2.2.0+1
  url_launcher: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

  mobx_codegen: ^2.4.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
